name: Lint
on:
  push:
    branches : [ main ]
    paths:
      - readme.md
  pull_request:
    branches: [ main ]
    paths:
      - readme.md

jobs:
  Awesome_Lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: npx awesome-lint